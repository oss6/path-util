'use strict';

var assert = require('assert');
var pathUtil = require('../lib');

describe('path-util', function () {

  describe('canonicalize', function () {
    it('should canonicalize correctly the given path', function () {
      assert(true, 'ok');
    });
  });

  describe('normalize', function () {
    it('should normalize the path with forward slashes', function () {
      assert(true, 'we expected this package author to add actual unit tests.');
    });
  });

  describe('getDirectory', function () {
    it('should retrieve the directory of a path', function () {
      assert(true, 'we expected this package author to add actual unit tests.');
    });
  });

  describe('makeRelative', function () {

  });

});
